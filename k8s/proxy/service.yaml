apiVersion: v1
kind: Service
metadata:
  name: {{NAMESPACE}}
  namespace: {{PREFIX}}-{{NAMESPACE}}
spec:
  type: LoadBalancer

  selector:
    app: {{NAMESPACE}}

  ports:
    - name: minecraft
      port: 25565
      targetPort: 25565
      protocol: TCP

  externalTrafficPolicy: Local